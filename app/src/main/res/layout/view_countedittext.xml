<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="wrap_content"
    android:layout_height="50dp"
    android:background="@drawable/shape_circle_rectangle"
    android:orientation="horizontal" >

    <ImageView
        android:id="@+id/count_sub"
        android:layout_width="0dp"
        android:layout_weight="1"
        android:layout_height="match_parent"
        android:contentDescription="@null"
        android:padding="8dp"
        android:src="@mipmap/icon_subtract_disable" />

    <View
        android:layout_width="1px"
        android:layout_height="match_parent"
        android:background="#969696" />

    <EditText
        android:id="@+id/count_edit"
        android:layout_width="100dp"
        android:layout_height="match_parent"
        android:background="@null"
        android:focusable="true"
        android:selectAllOnFocus="true"
        android:inputType="numberDecimal"
        android:textSize="26sp"
        android:maxLength="8"
        android:textColor="@color/black"
        android:gravity="center"/>

    <View
        android:layout_width="1px"
        android:layout_height="match_parent"
        android:background="#969696" />

    <ImageView
        android:id="@+id/count_add"
        android:layout_width="0dp"
        android:layout_weight="1"
        android:layout_height="match_parent"
        android:contentDescription="@null"
        android:padding="8dp"
        android:src="@mipmap/icon_red_cross_enable" />

</LinearLayout>